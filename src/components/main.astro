---

---

<div class="box">
  <slot name="show" />
  <div class="box2">
    <!-- 头像区域 -->
    <div class="userInfo">
      <a href="/about">
        <img src="../../src/assets/imgs/OIP2.jpg" alt="头像" />
      </a>
    </div>

    <!-- 标签区域 -->
    <slot name="tags" />
  </div>
</div>

<style lang="less" scoped>
  .userInfo {
    position: relative;
    top: -180px;
    width: 300px;
    height: 300px;
    border-radius: 10px;
    background-color: rgba(255, 255, 255, 0.7);
    box-shadow: 3px 5px 5px 0px #888;
    backdrop-filter: blur(14px) saturate(180%);
    padding: 10px;
    box-sizing: border-box;
    a {
      position: absolute;
      width: 280px;
      height: 280px;
      border-radius: 10px;
      object-fit: cover;
      img {
        width: 100%;
        border-radius: 10px;
        box-shadow: 0px 0px 4px 3px rgba(85, 81, 81, 0.5);
      }
    }
  }

  .box {
    display: flex;
    flex-direction: row-reverse;
  }

  .box2 {
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
    width: 300px;
  }
  @media screen and (max-width: 720px) {
    .box {
      display: flex;
      flex-direction: column-reverse;
      flex-wrap: nowrap;
      align-items: center;
    }
  }
</style>
